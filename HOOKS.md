# 等待你处理：问号动画

`waiting` 为琥珀色6×6问号，入场逐点绘制一次，随后以 2.4 秒为周期呼吸。
自动跟随优先级：等待处理 > 执行中 > 终止错误 > 完成。等待期间持续循环；结束后恢复其余任务的状态。手动试播按 `resultDisplayMs` 结束。

## 安装权限监听

桌面版请优先使用应用内首次设置入口；下述根目录脚本适用于源码或便携模式。

Windows 用户可直接双击根目录的 **setup-hooks.cmd**。它自动定位便携 Node 和可运行的 Codex CLI，合并配置后打开 CLI；然后输入 `/hooks` 并按提示审阅。不会替你按下信任，也不会绕过信任检查。CLI 查找支持 PATH 中的可执行程序以及 Codex 桌面客户端的本地版本目录；找不到时会明确提示，不修改系统 PATH。

安装器现在会先通过 Codex 的只读 `hooks/list` 接口核对实际加载的命令、事件、匹配条件和信任状态，而不只检查文件是否存在：

- 六项已信任且启用：显示配置完成，不再打开 CLI 要求重复审阅。
- 存在其他钩子：明确提示不要使用总览的 Trust all，逐项审阅本应用的六项。仅名称相同、命令不同的条目不会被认作本应用。
- 只发现本应用六项：提示核对原生界面实际列表后决定是否全部信任。
- 状态接口不支持、配置有警告或不能准确识别全部六项：报错结束，不把未知状态当作安全的全部信任。
- 从 CLI 退出后再次检查并显示结果。检查只适用于此次安装器的配置和工作目录；已有桌面任务是否重新加载、真实事件是否触发仍需验证。

Codex 原生 `/hooks` 目前是统一列表，本安装器不能保证隐藏其他来源的条目。不会临时删除其他配置、替换 CODEX_HOME、篡改信任数据库或自动按下信任。查询接口是实验性接口，Codex 版本变化可能需要适配。

桌面版日常直接启动 YOGO Studio，无需重复安装 hooks。别人首次使用需要在自己的 Codex 中完成信任；你的信任不会随源码分发。配置定义未变化时通常无需重复信任。以下命令方式仍可使用。

Windows便携包在项目目录运行：

```powershell
.\runtime\node.exe scripts/install-hooks.cjs
```

源码安装可用 `node scripts/install-hooks.cjs`。脚本向当前用户 `CODEX_HOME/hooks.json`（默认 `~/.codex/hooks.json`）合并本应用的显示钩子，保留其他钩子，修改前备份。不会改权限策略、信任记录或开机自启。

之后在支持 hooks 的 Codex CLI 中使用 `/hooks`，审阅并信任标为 `Yogo 75 codex status display` 的定义，再重启或恢复 Codex 会话。未信任的定义会被 Codex 跳过；安装脚本执行成功不等于权限联动已生效。不同客户端及版本仍需实测，不能仅凭配置文件断言桌面客户端已接通。

移动整个文件夹或更换 Node 路径后重新运行安装脚本，并重新审阅发生变动的定义。卸载此联动：

```powershell
.\runtime\node.exe scripts/install-hooks.cjs --remove
```

退出播放器后钩子静默返回，不会自动拉起播放器。不需要 ATK 客户端。

## 信号来源与边界

- `PermissionRequest` 开始显示问号。它表示即将请求权限；其他策略钩子仍可能直接批准或拒绝。
- 同一任务、同一轮、相同工具和参数摘要的 `PostToolUse` 撤掉问号。官方目前没有通用的“用户点击批准”完成钩子，所以获准后执行很久的命令，问号可能持续至命令返回；拒绝后若无工具返回，则由 Stop、Interrupt、SessionEnd 或该轮日志终止事件清除。
- 计划模式的阻塞式 `request_user_input` 使用 PreToolUse / PostToolUse，并支持从本地会话日志中匹配工具调用和对应的返回记录。普通文字中的问句不会触发；返回可以是回答、跳过或工具失败，均表示调用已不再阻塞。
- 桌面的异步 `request_user_input_async` 会立即返回 accepted，不能把这个返回当作回答；本版不将该异步接口当作阻塞式计划问题。需要单独适配其后续答复协议。
- 开启时已挂起的权限申请不会重新发送钩子，因此请先启动播放器再触发权限申请。阻塞提问的日志可恢复最近的等待状态。日志不是稳定API，客户端升级后可能需要更新。
- 本地监听范围不包含远端云任务。子代理钩子可能使用父会话标识，无法保证不同子代理的相同请求都可独立区分；完全相同并行请求也会合并。

钩子最多读取1MiB输入，转成事件名、会话/轮次标识和参数SHA-256摘要后，通过已有的本地令牌接口转交。命令、问题、回答不落盘、不出机。钩子无任何标准输出，退出码为0，不作授权决定。正常运行会有短暂 Node 进程；Windows还通过系统PowerShell安全启动，未新增常驻服务。显示期间仍会持续发送键盘帧，可能影响休眠。

## 验证

`npm test` 覆盖权限请求/结束、计划提问/回答、全局优先级、取消、跨轮清除、重复事件、无关输出、问号像素及循环、等待不按15秒结束、安装合并/卸载。

完整实机验收还需分别在支持且已信任钩子的 Codex 会话中触发真实权限提示和计划提问，观察问号及后续恢复。合成事件注入和手动动画播放只能验证显示通路，不能代替客户端实际触发验收。

官方接口与信任说明：[OpenAI Docs — Hooks](https://learn.chatgpt.com/docs/hooks)。
