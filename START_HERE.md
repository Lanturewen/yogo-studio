# 快速开始

1. 从项目 Releases 安装 YOGO Studio：Mac 将应用放入“应用程序”，Windows 运行 Setup.exe。打开应用并连接 YOGO 75 Pro 的 USB 或 2.4G 接收器。
2. 在首页确认设备与 Codex 任务来源。先预览灯效，再选择真实试播或恢复自动跟随。
3. 按需要开启背光、语音和波轮。首次配置可以把本目录与 AI_SETUP.md 交给能操作本机的 AI。

## 首次设置

| 功能 | Mac | Windows |
| --- | --- | --- |
| 微信输入法语音快捷键 | 右 Option | Ctrl+F5 |
| Codex 降低推理强度 | Control+Option+Shift+- | Ctrl+Alt+Shift+- |
| Codex 提高推理强度 | Control+Option+Shift+= | Ctrl+Alt+Shift+= |

先检查现有快捷键，不一致时在相应软件设置中手动录入。应用不会自动改写这两个软件的快捷键。

Mac 在“系统设置 → 隐私与安全性”按功能开启 YOGO Studio 的辅助功能、输入监控和麦克风权限。更新后如果已有授权失效，在系统列表中移除旧条目，重新添加“应用程序”里的新版；不需要关闭系统保护。

在官方键盘插件中将波轮左转映射为 `-`、右转映射为 `=`、按下映射为 `Enter`。两颗底部自定义键也由官方插件设置。YOGO Studio 不检查或写入键盘映射，只在 Codex 前台把无修饰键 `-` / `=` 转成推理强度快捷键；因此开启联动时，在 Codex 中直接输入这两个字符也会被拦截。

波轮联动开启时，Codex 前台的普通 `-` / `=` 也会用于调档；要正常输入这两个字符，可暂时关闭波轮联动。权限等待问号是可选的 hooks 集成，见 HOOKS.md。

## 日常使用

- 关闭窗口会保留后台联动；从应用菜单退出会释放设备。
- 升级前退出旧应用，然后安装新版本；默认保留个人配置。
- 蓝牙不支持灯效通信；Mac 提供 Apple Silicon 与 Intel 两种安装包，请选择与电脑处理器一致的版本。
- 若系统拦截未公证或未签名的安装包，先确认来自本项目 Releases，再通过系统正常安全界面处理。
