# Mac 使用说明

提供 Apple Silicon 安装包，微信输入法语音监听要求 macOS 14.2 或更新版本。安装及首次绑定见 START_HERE.md；AI 配置见 AI_SETUP.md。

按需要在“系统设置 → 隐私与安全性”允许辅助功能、输入监控和麦克风。辅助功能状态在后台静默检查，缺少权限时显示错误，不反复弹窗。若更新后系统授权绑定了旧签名，请移除旧条目，重新添加“应用程序”中的 YOGO Studio。

在官方键盘插件中把波轮左转设为 `-`、右转设为 `=`、按下设为 `Enter`；底部自定义键也由官方插件设置。YOGO Studio 不修改键盘映射，只在 Codex 前台把无修饰键 `-` / `=` 转成已配置的推理强度快捷键；需要输入这些字符时可关闭联动。请在 Codex → 设置 → 键盘快捷键中，将降低/提高推理强度分别绑定为 `Control+Option+Shift+-` / `Control+Option+Shift+=`。微信输入法语音快捷键设为右 Option。

微信输入法收音期间使用系统默认麦克风计算 RMS 音量，音频只暂存内存，不保存或上传。若输入法单独选择了其他麦克风，请使系统默认输入一致。拒绝麦克风权限时可保留收音状态提示，但不能得到真实音量。

当前包使用完整 ad-hoc 签名，未进行 Apple Developer ID 签名或公证。只从项目 Releases 下载，并通过系统正常界面处理首次启动提示；无需关闭系统保护。

在“偏好设置 → 运行方式”中可以关闭“在 Dock 显示图标”。设置保存后立即生效，重启后仍保持；隐藏 Dock 图标时，可从菜单栏的 YOGO Studio 图标打开窗口或退出应用。
